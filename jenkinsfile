pipeline {
    agent any

    stages {
        stage('Install dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing application..'
            }
        }
        stage('Deploy nodejs') {
            steps {
                echo 'Deploying ....'
            }
        }
    }
}